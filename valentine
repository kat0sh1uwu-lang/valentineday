<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>💕 Валентинка для Сонечки 💕</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Comfortaa:wght@400;700&family=Pacifico&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #1a0011, #2d0a1e, #1a0011);
            font-family: 'Comfortaa', cursive;
            overflow: hidden;
            position: relative;
        }

        .hearts-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
            overflow: hidden;
        }

        .floating-heart {
            position: absolute;
            top: -50px;
            font-size: 20px;
            animation: fall linear infinite;
            opacity: 0.6;
        }

        @keyframes fall {
            0% { transform: translateY(-50px) rotate(0deg); opacity: 0; }
            10% { opacity: 0.6; }
            90% { opacity: 0.6; }
            100% { transform: translateY(100vh) rotate(360deg); opacity: 0; }
        }

        .container {
            position: relative;
            z-index: 1;
            text-align: center;
            padding: 40px;
            max-width: 600px;
            width: 90%;
        }

        .card {
            background: linear-gradient(145deg, rgba(255, 255, 255, 0.1), rgba(255, 100, 150, 0.1));
            backdrop-filter: blur(10px);
            border: 2px solid rgba(255, 100, 150, 0.3);
            border-radius: 30px;
            padding: 50px 40px;
            box-shadow: 
                0 0 40px rgba(255, 50, 100, 0.2),
                0 0 80px rgba(255, 50, 100, 0.1),
                inset 0 0 40px rgba(255, 50, 100, 0.05);
            animation: cardPulse 3s ease-in-out infinite;
        }

        @keyframes cardPulse {
            0%, 100% { box-shadow: 0 0 40px rgba(255, 50, 100, 0.2), 0 0 80px rgba(255, 50, 100, 0.1); }
            50% { box-shadow: 0 0 60px rgba(255, 50, 100, 0.3), 0 0 120px rgba(255, 50, 100, 0.15); }
        }

        .big-heart {
            font-size: 80px;
            animation: heartbeat 1.5s ease-in-out infinite;
            display: inline-block;
            margin-bottom: 20px;
        }

        @keyframes heartbeat {
            0%, 100% { transform: scale(1); }
            15% { transform: scale(1.15); }
            30% { transform: scale(1); }
            45% { transform: scale(1.1); }
        }

        .question {
            font-family: 'Pacifico', cursive;
            font-size: 32px;
            color: #ff6b9d;
            margin-bottom: 35px;
            text-shadow: 0 0 20px rgba(255, 107, 157, 0.5);
            line-height: 1.4;
        }

        .buttons-container {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            justify-content: center;
            margin-top: 20px;
        }

        .btn-no {
            background: linear-gradient(145deg, #666, #444);
            color: #ccc;
            border: none;
            padding: 12px 24px;
            border-radius: 50px;
            font-family: 'Comfortaa', cursive;
            font-size: 14px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        }

        .btn-no:hover {
            transform: scale(0.95);
            opacity: 0.7;
        }

        .btn-yes {
            background: linear-gradient(145deg, #ff4081, #ff1744);
            color: white;
            border: none;
            padding: 18px 40px;
            border-radius: 50px;
            font-family: 'Comfortaa', cursive;
            font-size: 18px;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 25px rgba(255, 64, 129, 0.5);
            animation: yesGlow 2s ease-in-out infinite;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .btn-yes:hover {
            transform: scale(1.1);
            box-shadow: 0 6px 35px rgba(255, 64, 129, 0.7);
        }

        @keyframes yesGlow {
            0%, 100% { box-shadow: 0 4px 25px rgba(255, 64, 129, 0.5); }
            50% { box-shadow: 0 4px 40px rgba(255, 64, 129, 0.8), 0 0 60px rgba(255, 64, 129, 0.3); }
        }

        .btn-appear {
            animation: popIn 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        }

        @keyframes popIn {
            0% { transform: scale(0); opacity: 0; }
            100% { transform: scale(1); opacity: 1; }
        }

        .yes-page {
            display: none;
            text-align: center;
        }

        .yes-page.active {
            display: block;
            animation: fadeInUp 1s ease;
        }

        @keyframes fadeInUp {
            0% { opacity: 0; transform: translateY(30px); }
            100% { opacity: 1; transform: translateY(0); }
        }

        .yes-title {
            font-family: 'Pacifico', cursive;
            font-size: 42px;
            color: #ff4081;
            margin-bottom: 30px;
            text-shadow: 0 0 30px rgba(255, 64, 129, 0.6);
            animation: celebrateTitle 1s ease-in-out infinite alternate;
        }

        @keyframes celebrateTitle {
            0% { transform: scale(1); }
            100% { transform: scale(1.05); }
        }

        .love-text {
            color: #f0c0d0;
            font-size: 16px;
            line-height: 2;
            margin-bottom: 25px;
            text-align: left;
            padding: 0 10px;
        }

        .love-text p {
            margin-bottom: 15px;
        }

        .signature {
            font-family: 'Pacifico', cursive;
            font-size: 22px;
            color: #ff6b9d;
            margin-top: 30px;
            text-shadow: 0 0 15px rgba(255, 107, 157, 0.4);
        }

        .emoji-rain {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 100;
        }

        .confetti {
            position: absolute;
            font-size: 30px;
            animation: confettiFall linear forwards;
        }

        @keyframes confettiFall {
            0% { transform: translateY(-50px) rotate(0deg) scale(0); opacity: 1; }
            20% { transform: translateY(10vh) rotate(90deg) scale(1.2); opacity: 1; }
            100% { transform: translateY(100vh) rotate(720deg) scale(0.5); opacity: 0; }
        }

        .counter {
            color: rgba(255, 150, 180, 0.6);
            font-size: 12px;
            margin-top: 15px;
        }

        .hint {
            color: #ff6b9d;
            font-size: 14px;
            margin-top: 10px;
            font-style: italic;
            opacity: 0;
            transition: opacity 0.5s;
        }

        .hint.visible {
            opacity: 1;
        }

        @media (max-width: 480px) {
            .card { padding: 30px 20px; }
            .question { font-size: 24px; }
            .yes-title { font-size: 32px; }
            .love-text { font-size: 14px; }
            .big-heart { font-size: 60px; }
        }

        .hearts-border {
            display: flex;
            justify-content: center;
            gap: 5px;
            font-size: 20px;
            margin: 15px 0;
        }

        .hearts-border span {
            animation: heartWave 2s ease-in-out infinite;
        }

        @keyframes heartWave {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-8px); }
        }

        .sparkle {
            position: absolute;
            width: 4px;
            height: 4px;
            background: #ff6b9d;
            border-radius: 50%;
            pointer-events: none;
            animation: sparkleAnim 0.6s ease-out forwards;
        }

        @keyframes sparkleAnim {
            0% { transform: scale(0); opacity: 1; }
            100% { transform: scale(3); opacity: 0; }
        }
    </style>
</head>
<body>

    <div class="hearts-bg" id="heartsBg"></div>

    <div class="container">
        <div class="card" id="questionCard">
            <div class="big-heart">💕</div>
            <div class="question">Ты станешь моей Валентинкой? 🥺</div>
            
            <div class="buttons-container" id="buttonsContainer">
                <button class="btn-no" onclick="addNoButton()">Нет</button>
                <button class="btn-no" onclick="addNoButton()">Нет</button>
            </div>

            <div class="hint" id="hint"></div>
            <div class="counter" id="counter"></div>
        </div>

        <div class="yes-page" id="yesPage">
            <div class="card">
                <div class="yes-title">УРААА! 🎉💖🎉</div>
                
                <div class="hearts-border" id="heartsWave"></div>

                <div class="love-text">
                    <p>Сонечка любимая, уже наше второе 14 февраля! 💕</p>
                    
                    <p>Я тебя очень сильно люблю и буду любить вечно. Целую вечность. ❤️😘</p>
                    
                    <p>У меня не было никогда в планах переставать тебя любить, специально тебе нагадить или что еще хуже расстаться (ни единой мысли такой не было) 🙅‍♂️💘</p>
                    
                    <p>За этот период мы стали еще ближе друг к другу во всех планах, буквально дополняем друг друга и мне это так нравится, я очень рад что с тобой познакомился и предложил встречаться 🥰✨</p>
                </div>

                <div class="hearts-border" id="heartsWave2"></div>

                <div class="signature">
                    💝 Навечно любимой Сонечке 💝
                </div>
            </div>
        </div>
    </div>

    <div class="emoji-rain" id="emojiRain"></div>

    <script>
        const noButtonTexts = [
            "Неа 😤",
            "Даже не думай!",
            "Ни за что! 🙅‍♀️",
            "Попробуй ещё раз 😏",
            "Хм, дай подумать... НЕТ",
            "А если я скажу нет? 🤔",
            "Не сегодня! 😜",
            "Ты серьёзно? 😂",
            "Может быть... нет 💅",
            "Неееет 🙈",
        ];

        const hintTexts = [
            "",
            "",
            "Ну камооон 😏",
            "",
            "Подсказка: правильный ответ не 'нет' 💕",
            "",
            "Ты точно уверена? 🤨",
            "",
            "Осталось совсем чуть чуть... 👀",
            "Последний шанс сказать нет! 😜",
        ];

        let clickCount = 0;
        let currentNoIndex = 0;

        function addNoButton() {
            clickCount++;

            createSparkles(event);

            const counter = document.getElementById('counter');
            counter.textContent = `Нажатий "нет": ${clickCount} 💔`;

            const hint = document.getElementById('hint');
            if (clickCount <= hintTexts.length && hintTexts[clickCount - 1]) {
                hint.textContent = hintTexts[clickCount - 1];
                hint.classList.add('visible');
            }

            if (event && event.target) {
                event.target.style.fontSize = '12px';
                event.target.style.padding = '8px 16px';
                event.target.style.opacity = '0.5';
                event.target.disabled = true;
            }

            const container = document.getElementById('buttonsContainer');

            // После 10 нажатий показываем кнопку ДА
            if (clickCount >= 10) {
                // Убираем все кнопки нет
                container.innerHTML = '';

                const yesBtn = document.createElement('button');
                yesBtn.className = 'btn-yes btn-appear';
                yesBtn.textContent = '💖 ДА КОНЕЧНО СТАНУ! 💖';
                yesBtn.onclick = showYesPage;
                container.appendChild(yesBtn);

                hint.textContent = 'Вот видишь, другого варианта и не было 😘';
                hint.classList.add('visible');

                return;
            }

            // Добавляем новую кнопку нет
            if (currentNoIndex < noButtonTexts.length) {
                const newBtn = document.createElement('button');
                newBtn.className = 'btn-no btn-appear';
                newBtn.textContent = noButtonTexts[currentNoIndex];
                newBtn.onclick = function() { addNoButton(); };
                container.appendChild(newBtn);
                currentNoIndex++;
            }
        }

        function createSparkles(e) {
            if (!e) return;
            for (let i = 0; i < 6; i++) {
                const sparkle = document.createElement('div');
                sparkle.className = 'sparkle';
                sparkle.style.left = (e.clientX + (Math.random() - 0.5) * 40) + 'px';
                sparkle.style.top = (e.clientY + (Math.random() - 0.5) * 40) + 'px';
                sparkle.style.background = ['#ff4081', '#ff6b9d', '#ffb3d1', '#ff1744'][Math.floor(Math.random() * 4)];
                document.body.appendChild(sparkle);
                setTimeout(() => sparkle.remove(), 600);
            }
        }

        function showYesPage() {
            document.getElementById('questionCard').style.display = 'none';
            
            const yesPage = document.getElementById('yesPage');
            yesPage.classList.add('active');

            generateHeartsWave('heartsWave');
            generateHeartsWave('heartsWave2');

            launchConfetti();

            for (let i = 0; i < 30; i++) {
                setTimeout(() => createFallingHeart(), i * 100);
            }
        }

        function generateHeartsWave(containerId) {
            const container = document.getElementById(containerId);
            const hearts = ['💕', '💖', '💗', '💘', '💝', '❤️', '💞', '💓', '💕'];
            hearts.forEach((heart, i) => {
                const span = document.createElement('span');
                span.textContent = heart;
                span.style.animationDelay = (i * 0.15) + 's';
                container.appendChild(span);
            });
        }

        function launchConfetti() {
            const emojis = ['💖', '💕', '❤️', '💗', '🎉', '✨', '💘', '🥰', '💝', '🌹', '💐', '🎊'];
            const container = document.getElementById('emojiRain');
            
            for (let i = 0; i < 60; i++) {
                setTimeout(() => {
                    const confetti = document.createElement('div');
                    confetti.className = 'confetti';
                    confetti.textContent = emojis[Math.floor(Math.random() * emojis.length)];
                    confetti.style.left = Math.random() * 100 + 'vw';
                    confetti.style.fontSize = (20 + Math.random() * 25) + 'px';
                    confetti.style.animationDuration = (2 + Math.random() * 3) + 's';
                    container.appendChild(confetti);
                    setTimeout(() => confetti.remove(), 5000);
                }, i * 80);
            }

            setTimeout(() => {
                for (let i = 0; i < 30; i++) {
                    setTimeout(() => {
                        const confetti = document.createElement('div');
                        confetti.className = 'confetti';
                        confetti.textContent = emojis[Math.floor(Math.random() * emojis.length)];
                        confetti.style.left = Math.random() * 100 + 'vw';
                        confetti.style.fontSize = (20 + Math.random() * 20) + 'px';
                        confetti.style.animationDuration = (2 + Math.random() * 3) + 's';
                        container.appendChild(confetti);
                        setTimeout(() => confetti.remove(), 5000);
                    }, i * 150);
                }
            }, 3000);
        }

        function createFallingHeart() {
            const container = document.getElementById('heartsBg');
            const hearts = ['❤️', '💕', '💖', '💗', '💓', '♥️'];
            const heart = document.createElement('div');
            heart.className = 'floating-heart';
            heart.textContent = hearts[Math.floor(Math.random() * hearts.length)];
            heart.style.left = Math.random() * 100 + '%';
            heart.style.fontSize = (12 + Math.random() * 20) + 'px';
            heart.style.animationDuration = (4 + Math.random() * 6) + 's';
            heart.style.animationDelay = Math.random() * 2 + 's';
            container.appendChild(heart);
            setTimeout(() => heart.remove(), 12000);
        }

        function initBackgroundHearts() {
            for (let i = 0; i < 15; i++) {
                setTimeout(() => createFallingHeart(), i * 500);
            }
            setInterval(() => createFallingHeart(), 800);
        }

        initBackgroundHearts();
    </script>
</body>
</html>
